COGNITHOR_BIND_IP=127.0.0.1
COGNITHOR_API_PORT=8741
JARVIS_LANGUAGE=en
JARVIS_LOGGING_LEVEL=INFO

# If Ollama is running on the same host machine outside the container,
# this default usually works with Podman.
JARVIS_OLLAMA_BASE_URL=http://host.containers.internal:11434
